{
  "name": "string-calculator",
  "version": "1.0.0",
  "scripts": {
    "test": "jest",
    "tcr": "jest && git commit -am 'Commit' || git reset --hard"
  },
  "devDependencies": {
    "@babel/plugin-transform-modules-commonjs": "^7.4.4",
    "jest": "^24.8.0"
  }
}
